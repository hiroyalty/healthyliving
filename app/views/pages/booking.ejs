<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../partials/head %>
</head>
<body class="container">

	<header>
		<% include ../partials/header %>
	</header>

	<main>
		<div class="container">			
			<div class="col-sm-6.col-md-4.col-md-offset-4">     
                    
                        <% if(user!=null){ %>
                            <h1 class="text-center">Book Appointment <%= user %></h1>
                                <% if(messagedrop!=null){ %>
                                <h4 class="text-center" style="color: #00B7FF"><%= messagedrop %></h4>
                                <%}%>
				<div class="signup-wall">
                <form class="form-signin", data-toggle="validator", action="/book", method="POST">
					<div class="form-group">
					<label for="formGroupExampleInput">we will respond latest in 3 working days.</label>
					<input class="form-control" type="text" id="username" name="username" value=<%= user %> readonly></input>
					</div>
					<div class="form-group">
					<label for="bookcategory">Select your area of concern</label>
					<select class="form-control" name="bookcategory", id="bookcategory", required>
					<option value="blood pressure">Blood Pressure</option>
					<option value="cholesterol control">Cholesterol Control</option>
					<option value="blood sugar">Blood Sugar</option>
					<option value="loose weight">Loose Weight</option>
					<option value="get active">Get Active</option>
					<option value="general">General</option>
					</select>
				</div>
                <div class="form-group">
					<label for="bookdetails">Write your Message Here</label>
                    <textarea class="form-control" id="bookdetails" name="bookdetails", rows="5", required></textarea>
				</div>
				<div class="form-group">
				<div>To come in for checkup, Make Appointment!</div>
				<div class="radio">
					<label>
					<input type="radio" name="bookappointment" id="bookappointment" value="yes" required>
					Yes
					</label>
				</div>
				<div class="radio">
					<label>
					<input type="radio" name="bookappointment" id="bookappointment" value="no" required>
					No
					</label>
				</div>
				</div>
				<button class="btn btn-primary", type="submit">Submit</button>
				</form>
					<span class="clearfix"></span>
					<%}%> 
					</div>
                </div>
		</div>
	</main>

	<footer>
		<% include ../partials/footer %>
	</footer>
	
</body>
</html>